import styled from "styled-components";

const RecommendedWorksContainer = styled.div`
  max-width: 1024px;
  margin: 0px auto;

  .arts {
    margin-top: 12px;
    display: flex;
    gap: 26px;

    flex-wrap: wrap;
  }
`;

export default RecommendedWorksContainer;
